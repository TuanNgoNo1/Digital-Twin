using UnityEngine;

/// <summary>
/// Marks a printed/non-interactive socket position that connected wires must
/// avoid. It intentionally has no collider or input behaviour.
/// </summary>
public sealed class WireRoutingObstacle : MonoBehaviour
{
}
